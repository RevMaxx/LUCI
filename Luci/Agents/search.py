from Core.search_text import search_text_async, search_text, print_text_result
from Core.search_image import search_images_async, search_images, print_img_result

class Search:
    def __init__(self, query: str) -> str:
        self.query=query

    async def search(query, search_text):
        return await.self.query.

